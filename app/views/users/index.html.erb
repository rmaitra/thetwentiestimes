
<%= button_to "Add", new_user_path, :method => :get, :class => "btn btn-primary" %>
</br>
</br>
<div ng-controller="UsersCtrl">
    <table class = "table table-bordered table-hover table-striped">
      <thead>
        <tr>
            <th>Name<th>
        </tr>
      </thead>
      <tbody>
        <tr ng-repeat="user in users">
            <td><a ng-click ="viewUser($index)">{{user.name}}</a><td>
            <td><button class = "btn btn-primary" ng-click ="destroy($index)">Delete</button><td>
            <td><button class = "btn btn-primary" ng-click ="editUser($index)">Edit</button><td>
        </tr>
      </tbody>
</div>




