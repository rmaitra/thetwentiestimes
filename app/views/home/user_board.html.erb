
<div class = "container" ng-controller="UsersCtrl">
    <div class = "pull-right">
        <b>Search:</b> <input ng-model="searchText">
    </div>
    
    </br>
    </br>
    
    
    <div class="row" >
        <div class="col-sm-4 col-md-4" ng-repeat="user in users | filter:searchText | orderBy:predicate:reverse">
            <div class="thumbnail">
            <img data-src="holder.js/300x200" alt="...">
            <div class="caption">
                <h3><a href ="/profile?id={{user.id}}">{{user.name}}</a></h3>
            </div>
            </div>
        </div>
    </div>

    <!--<table class = "table table-hover table-striped">
      <thead>
        <tr>
            <th><a href="" ng-click="predicate = 'name'; reverse=false">Name</a>
            (<a href="" ng-click="predicate = '-name'; reverse=false">^</a>)</th>
        </tr>
      </thead>
      <tbody>
        <tr ng-repeat="user in users | filter:searchText | orderBy:predicate:reverse">
            <td><a href ="/profile?id={{user.id}}">{{user.name}}</a></td>
        </tr>
      </tbody>
    </table>-->
</div>